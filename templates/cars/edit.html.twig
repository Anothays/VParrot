{% extends 'base.html.twig' %}

{% block title %}Edit Cars{% endblock %}

{% block main %}
    <h1 class="text-bg-primary">éditer véhicule</h1>
    <div class="form w-25 bg-dark-subtle">
        {{ include('cars/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <a class="btn btn-dark" href="{{ path('app_cars_index') }}">Retour</a>
        {{ include('cars/_delete_form.html.twig') }}
    </div>
{% endblock %}
